
import React from "react";
import Layout from "@/components/Layout";

const ChatPage = () => {
  return (
    <Layout>
      <div className="flex items-center justify-center h-full">
        <p className="text-muted-foreground">Chat page will be redesigned.</p>
      </div>
    </Layout>
  );
};

export default ChatPage;
